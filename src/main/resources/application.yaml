server:
  port: ${APP_PORT:8080}

spring:
  application:
    name: oneasset-api

  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: Asia/Seoul

  flyway:
    enabled: true
    baseline-on-migrate: true

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${COGNITO_ISSUER_URI}

  cloud:
    aws:
      region:
        static: ${AWS_REGION:ap-northeast-2}

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace

oneasset:
  api-key:
    server-secret: ${ONEASSET_API_KEY_SERVER_SECRET:local-dev-api-key-secret}
  storage:
    asset-bucket: ${ONEASSET_ASSET_BUCKET:oneasset}
    region: ${AWS_REGION:ap-northeast-2}
    delivery-base-url: ${ONEASSET_DELIVERY_BASE_URL:}

app:
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:http://localhost:5174}
